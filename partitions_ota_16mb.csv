# Name,   Type, SubType,   Offset,    Size,      Flags
# ---------------------------------------------------------------------------
# Internal-/ext layout for 16 MB flash boards (LilyGo T-Embed CC1101).
#
# This feature branch intentionally uses a single factory app slot instead of
# dual OTA. The released flash space becomes a large FATFS volume used as the
# transparent /ext fallback when no microSD can be mounted.
#
# Recovery remains available through the ESP32-S3 ROM download mode: erase the
# flash over USB and flash another firmware/partition table at any time.
#
# nvs keeps its established offset/size. The application gets 4.5 MiB (current
# firmware is ~3.2 MiB), while ext_fat gets 0xB50000 bytes (~11.31 MiB raw).
# The last 128 KiB remain reserved for coredumps. Wear levelling/FAT metadata
# consumes a small part of ext_fat, so usable capacity is slightly lower.
#
# With no OTA app partition esp_ota_get_next_update_partition() returns NULL,
# so the port's OTA updater reports OTA unsupported instead of touching /ext.
# ---------------------------------------------------------------------------
nvs,        data, nvs,       0x9000,    0x6000,
phy_init,   data, phy,       0xf000,    0x1000,
factory,    app,  factory,   0x10000,   0x480000,
ext_fat,    data, fat,       0x490000,  0xB50000,
coredump,   data, coredump,  0xFE0000,  0x20000,
